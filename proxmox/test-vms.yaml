# ==============================================================================
# Proxmox Test VM Definitions
# These MAC addresses match the configurations in config/hosts.yaml
# ==============================================================================

vms:
  - vmid: 9033
    name: "test-ubuntu-k3s"
    os: "ubuntu"
    mac: "bc:24:11:00:24:33"
    cores: 2
    memory: 10240 # MB (Cần 10GB khi boot mạng vì ISO 3.8GB tải tạm vào tmpfs 50% RAM, sau khi cài xong giảm lại 4GB)
    disk_size: 30 # GB
    bios: "ovmf" # 'ovmf' (UEFI) or 'seabios'
